services:
  - type: web
    name: better-predictions
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --bind 0.0.0.0:    envVars:
      - key: THESTATSAPI_KEY
        sync: false
